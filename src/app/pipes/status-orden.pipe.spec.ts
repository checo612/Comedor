import { StatusOrdenPipe } from './status-orden.pipe';

describe('StatusOrdenPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusOrdenPipe();
    expect(pipe).toBeTruthy();
  });
});
